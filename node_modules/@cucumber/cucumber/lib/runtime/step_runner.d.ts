import { type PickleStep, type TestStepResult } from '@cucumber/messages';
import type { IDefinition } from '../models/definition';
import type { ITestCaseHookParameter } from '../support_code_library_builder/types';
export interface IRunOptions {
    defaultTimeout: number;
    filterStackTraces: boolean;
    hookParameter: ITestCaseHookParameter;
    step: PickleStep;
    stepDefinition: IDefinition;
    world: any;
}
export interface RunStepResult {
    result: TestStepResult;
    error?: unknown;
}
export declare function run({ defaultTimeout, filterStackTraces, hookParameter, step, stepDefinition, world, }: IRunOptions): Promise<RunStepResult>;
declare const _default: {
    run: typeof run;
};
export default _default;
